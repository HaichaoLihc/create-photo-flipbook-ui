{
  "name": "create-photo-flipbook-website",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare-demo.mjs",
    "dev": "vite",
    "prebuild": "node scripts/prepare-demo.mjs",
    "build": "vite build",
    "preview": "vite preview --base /create-photo-flipbook-ui/ --host 127.0.0.1 --port 4173 --strictPort",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "quick_flipbook": "^1.1.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
